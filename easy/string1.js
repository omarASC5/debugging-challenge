function printOutHello(num){
  const message = "Hello
  World";
  if(num %2 === 0);{
    return "Goodbye, Mate"
  }
  return message;
};

// Expected result: "Hello World"

// That one is really broken! Please fix it
